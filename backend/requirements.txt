# ─────────────────────────────────────────────────────────────────────────────
# autoMITRE v1.2 — Python Backend Requirements
# Python 3.13+ required
# Install with:  pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.133.1
uvicorn[standard]==0.41.0
python-multipart==0.0.22
starlette==0.52.1
aiofiles==25.1.0

# ── Data Validation ───────────────────────────────────────────────────────────
pydantic==2.12.5
pydantic-settings==2.1.0
python-dotenv==1.2.1

# ── Authentication & Security ─────────────────────────────────────────────────
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
cryptography==46.0.5

# ── Database ──────────────────────────────────────────────────────────────────
SQLAlchemy==2.0.47
aiosqlite==0.22.1
greenlet==3.3.2

# ── HTTP Clients ──────────────────────────────────────────────────────────────
httpx==0.28.1
requests==2.32.5
aiohttp==3.13.3

# ── AI / ML Core ──────────────────────────────────────────────────────────────
torch==2.10.0
transformers==5.3.0
sentence-transformers==5.2.3
accelerate==1.13.0
safetensors==0.7.0
tokenizers==0.22.2
huggingface_hub==1.5.0

# ── NLP ───────────────────────────────────────────────────────────────────────
spacy==3.8.11
nltk==3.9.3
# spaCy English model is installed separately — see setup.sh

# ── Classical ML ──────────────────────────────────────────────────────────────
scikit-learn==1.8.0
xgboost==3.2.0
numpy==2.4.2
scipy==1.17.1
pandas==3.0.1
joblib==1.5.3

# ── Groq Cloud LLM (Stage 2 reasoning) ───────────────────────────────────────
groq==1.0.0

# ── Local LLM (Phi-3.5-mini, optional fallback) ───────────────────────────────
llama_cpp_python==0.3.16
ollama==0.6.1

# ── threat_pipeline: Layer 1 LLM-as-NLP extractor (Apple Silicon) ─────────────
# mlx-lm is the preferred local-inference backend on M-series Macs. If it is
# unavailable the extractor degrades to a deterministic regex parser, so this is
# effectively optional on non-Apple-Silicon hosts.
mlx-lm==0.31.3

# ── threat_pipeline: Layer 2 vector store (RAG retrieval) ─────────────────────
chromadb==1.5.9

# ── STIX / CTI Standards ──────────────────────────────────────────────────────
stix2==3.0.2
stix2-patterns==2.1.2

# ── Network / PCAP Analysis ───────────────────────────────────────────────────
scapy==2.7.0

# ── PDF / Report Generation ───────────────────────────────────────────────────
Jinja2==3.1.6
xhtml2pdf==0.2.17
reportlab==4.4.10
lxml==6.0.2
pdfminer.six==20251230
pdfplumber==0.11.9
pillow==12.1.1
svglib==1.6.0

# ── Data Processing ───────────────────────────────────────────────────────────
openpyxl==3.1.5
PyYAML==6.0.3
regex==2026.2.19
python-dateutil==2.9.0.post0
pytz==2025.2
packaging==26.0
tqdm==4.67.3
datasets==4.6.1
evaluate==0.4.6

# ── Email Validation ──────────────────────────────────────────────────────────
email-validator==2.3.0
dnspython==2.8.0

# ── Misc Utilities ────────────────────────────────────────────────────────────
rich==14.3.3
psutil==7.2.2
anyio==4.12.1
matplotlib>=3.7
